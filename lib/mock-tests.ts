import { TestMetadata } from '@/types/exam';

/**
 * Base mock tests - cleared for fully dynamic operation.
 * All tests are now created and published dynamically via the Admin Dashboard.
 */
export const MOCK_TESTS: TestMetadata[] = [];
